水质污染物数据分析平台 前端商业化大屏优化需求说明书（对标阿里 / 字节浅色调专业级标准）
核心原则
100% 保留原有 6 大业务模块（污染物分析、异常值分析、安全性分析、比例分析、关联规则分析、规则查询）的全部功能与数据逻辑，对标阿里云 DataWorks、字节火山引擎 DataWind的浅色调商业化专业大屏设计标准，打造「极简干净、层次清晰、专业克制、高可用性」的 To B 大屏产品，同时为后续新增功能预留完整的拓展空间。
第一部分：对标大厂的浅色调底层设计规范（优先落地，所有优化的基础）
本部分为大屏优化的底层统一规范，完全对齐阿里 Ant Design、字节 Arco Design 的企业级浅色调设计体系，落地后可保证全平台视觉风格统一，无业务功能改动。
1. 浅色调专业色彩体系（适配办公 / 汇报大屏场景）
表格
色彩类型	大厂级规范要求
品牌主色	选用阿里 / 字节 To B 产品通用的沉稳科技蓝（主色值：#1677FF），建立 9 级梯度色板，仅用于核心指标高亮、主操作按钮、选中态，禁止大面积滥用
业务功能色	严格对齐企业级状态色标准，适配浅色调场景：
1. 安全合规态：低饱和专业绿（#00B42A）
2. 异常超标态：警示红（#F53F3F）
3. 预警关注态：提醒橙（#FF7D00）
4. 中性信息：8 级梯度灰板（#F2F3F5-#1D2129），保证浅底色下的可读性
浅色调大屏背景规范	采用阿里 / 字节办公类大屏通用的纯净浅色系规范：
1. 页面底色：低饱和浅灰（#F5F7FA），无渐变，降低长时间办公观看的视觉疲劳
2. 卡片容器：纯白底色（#FFFFFF），统一 8px 圆角、轻量投影（0 2px 12px rgba (0,0,0,0.06)）、24px 内边距，仅在必要场景使用 1px #E5E6EB 浅灰边框
3. 分割线：仅用 1px #E5E6EB 浅灰线做区域分割，禁止冗余杂乱的线条
2. 企业级字体与排版规范
字体：选用阿里普惠体 / 字节跳动黑体，完全匹配大厂商用字体标准，建立 4 级固定字体层级，适配浅色调大屏的清晰阅读需求：
表格
层级	用途	字号 / 字重	规范要求
H1	大屏标题 / 核心指标数值	32px-40px / 700 字重	用品牌主色 / 深灰 #1D2129，保证远距离清晰识别
H2	模块标题 / 次级指标	18px-22px / 600 字重	深灰 #1D2129，与模块内容固定间距 16px
H3	图表标题 / 维度标签	14px-16px / 500 字重	中灰 #4E5969，左对齐展示
Body	正文 / 辅助数值	12px-14px / 400 字重	低灰 #86909C，数值统一右对齐
排版规则：所有内容严格遵循「标题顶对齐、重点数据左对齐、数值右对齐」的大厂排版标准，禁止出现参差不齐的文本排布。
3. 通用组件规范
完全复用阿里 Ant Design、字节 Arco Design 的企业级浅色调组件交互规范，统一封装按钮、表单、弹窗、tooltip 等通用组件，保证全平台交互体验一致，所有组件均适配浅色调模式，视觉风格完全统一。
本部分验收标准
全局设计 token（色彩、字体、间距、圆角）已通过 CSS 变量统一管理，可全局一键调整规范
所有通用组件已完成封装，视觉与交互完全对齐阿里 / 字节的企业级浅色调设计规范
规范可复用于后续所有页面、图表的优化，无风格冲突。
第二部分：大厂级浅色调大屏布局架构重构（第二批次落地，搭好核心框架）
本部分基于第一部分的规范，对标阿里 / 字节浅色调专业大屏的布局逻辑，重构页面架构，完全保留原有所有业务模块，仅调整布局优先级与信息层级，不涉及图表内部细节优化。
1. 大屏栅格规范
全页面采用 24 列栅格系统（大厂大屏通用标准），固定规则：
页面左右外边距统一为 32px
栅格间距（gutter）统一为 24px
所有卡片严格按栅格占比分配，保证像素级对齐，无错位情况
2. 大屏页面布局重构（完全保留原有业务模块）
对标阿里云 DataWorks、字节飞书 BI 的经典浅色调大屏布局逻辑，从上到下分为 4 个固定层级，原有 6 个业务模块 100% 覆盖：
表格
布局区域	栅格占比	对应原有业务模块	大厂级规范要求
顶部全局栏	24 列全宽	平台品牌区 + 全局操作区	左侧放置平台名称「水质污染物数据分析平台」，右侧放置全局时间筛选器、全屏按钮、数据导出按钮、用户信息，高度固定 72px，无冗余元素，适配浅色调规范
核心 KPI 指标区	24 列全宽，均分 4 列	新增核心指标卡	放置 4 个核心 KPI：总样本数、安全样本占比、异常值总数、超标污染物 TOP1，每个指标卡包含数值、同比变化、迷你趋势图，保证用户 1 秒掌握全局状态
中部核心分析区	左 12 列 + 右 12 列	上排左：比例分析（超标比例分析）、上排右：异常值分析（异常值统计）
下排左：污染物分析（污染物数值分布）、下排右：安全性分析（安全样本统计）	优先展示用户最关心的风险分析模块，每个模块固定卡片尺寸，标题统一在卡片左上角，保证视觉整齐划一，适配浅色调卡片规范
底部深度分析区	24 列全宽	关联规则分析（污染物关联规则散点图）	全宽展示深度分析模块，适配散点图的大数据量展示需求，卡片高度统一，与上方模块间距固定
侧边功能导航	左侧固定 240px 宽度	原有 6 大功能页面导航	固定悬浮于页面左侧，适配浅色调规范，选中态用品牌主色高亮，支持折叠，不占用核心大屏展示空间
3. 单功能页面统一布局规范
针对污染物分布、异常值分析等单独功能页面，统一采用大厂 BI 产品的浅色调标准布局：
顶部筛选区：24 列全宽，放置页面筛选条件
核心指标区：24 列全宽，放置该功能的核心 KPI 指标卡
图表分析区：按 24 列栅格规范放置分析图表
本部分验收标准
所有页面已按 24 列栅格规范重构，所有卡片像素级对齐，无布局错乱
大屏首页已按大厂布局逻辑完成重构，原有所有业务模块均完整保留，无功能缺失
所有单功能页面已按统一规范完成布局调整，全平台布局风格一致。
第三部分：对标大厂标准的浅色调图表可视化专业重构（第三批次落地，可单图表分批处理）
本部分针对每个业务模块的图表，对标阿里 AntV、字节 DataWind 的浅色调企业级图表最佳实践做优化，100% 保留原有数据与分析能力，仅优化可视化呈现，每个图表的优化需求完全独立，可分批落地。
表格
原有业务模块	对应图表	大厂级浅色调优化需求
污染物分析	污染物数值分布箱线图	1. 配色完全适配全局浅色调规范，箱体用品牌主色，异常值用警示红高亮
2. 优化 tooltip，hover 时完整展示最大值、最小值、中位数、上下四分位数的专业统计值，样式统一为大厂标准浅色调浮层
3. 去除冗余网格线，仅保留低透明度横向参考线，坐标轴标签用中性灰，保证不抢数据焦点
4. 优化 x 轴污染物名称展示，增加文字旋转适配，避免标签拥挤重叠
异常值分析	异常值统计柱状图	1. 按异常值数量从高到低降序排序，让用户快速定位最高风险项
2. 异常值 TOP3 的柱子用警示红高亮，其余柱子用品牌主色梯度色，视觉主次分明
3. 优化为横向柱状图，彻底解决 x 轴标签拥挤问题，符合大厂 BI 产品的最佳实践
4. 柱子顶部直接标注异常值数量，hover 时展示明细数据，无需额外对照坐标轴
安全性分析	安全样本统计饼图	1. 改为大厂商业 BI 通用的环形图，提升视觉高级感，适配浅色调大屏模式
2. 环形图中心直接标注总样本数，图内标注安全 / 不安全的数值与占比，无需额外对照图例
3. 配色严格适配全局安全绿 / 异常红的状态色规范，适配浅底色的视觉效果
4. 优化 tooltip，展示样本总数、安全 / 不安全样本的明细数值与占比
比例分析	超标比例分析双轴图	1. 主次指标视觉严格区分：超标次数用品牌主色柱状图作为主指标，超标比例用警示红折线作为次指标，弱化次要指标的视觉占比
2. 仅标注 TOP3 超标项的数值，去除密密麻麻的冗余标签，保证图表简洁专业
3. 双轴刻度、图例样式完全适配全局浅色调规范，坐标轴用低透明度线条，不抢数据焦点
4. 优化 hover 联动高亮，鼠标悬浮时同步展示对应污染物的超标次数与超标比例，浮层样式统一
关联规则分析	污染物关联规则散点图	1. 优化图例，清晰标注「点大小 = 支持度、颜色 = 提升度」的规则，放置在图表左上角，符合大厂规范
2. 按置信度区间做颜色梯度区分，高置信度点用品牌主色高亮，低置信度点用低饱和色，视觉层次清晰
3. 优化 tooltip，hover 时展示完整的关联规则明细、置信度、提升度、支持度数值，样式统一
4. 优化坐标轴刻度与标签，去除冗余网格线，适配浅色调大屏模式
关联规则查询	规则查询列表 / 图表	1. 表格样式完全适配全局浅色调规范，表头高亮，高置信度规则用品牌主色标注
2. 优化查询表单的样式，完全对齐企业级组件规范，操作体验流畅
3. 表格支持排序、筛选，符合大厂 BI 产品的通用交互标准
本部分验收标准
每个图表优化后，原有数据展示、分析能力 100% 保留，无数据错误
所有图表的样式完全适配全局浅色调规范，视觉风格统一，对齐阿里 / 字节的企业级图表标准
图表的可读性、专业度有明显提升，tooltip、标签展示正常，无拥挤、重叠问题。
第四部分：大厂级交互与体验优化（第四批次落地，不改动核心业务）
本部分为平台新增阿里 / 字节商业化 BI 产品的标准交互能力，完全基于原有业务数据，不改动后端接口与基础功能，可在基础页面、图表优化完成后落地。
1. 全局筛选与联动能力
新增全局时间筛选器，支持按日 / 周 / 月 / 年筛选，筛选后全页面所有图表联动刷新
新增全局污染物多选筛选器，支持用户自定义查看关注的污染物数据，所有图表同步更新
实现图表间的联动交互：点击柱状图的某一污染物，其他图表同步高亮该污染物的对应数据，符合大厂 BI 产品的联动标准
2. 专业级图表交互能力
所有图表支持 hover 高亮、点击下钻：点击超标柱状图 / 环形图，可弹窗展示对应污染物的明细数据、历史趋势，弹窗样式完全对齐企业级浅色调规范
所有图表新增全屏查看功能，点击可全屏展示该图表，适配汇报、投屏场景
新增图表数据导出功能，支持 Excel 格式导出明细数据、PNG/PDF 格式导出图表图片，符合商业化产品的标准能力
新增图表缩放、重置功能，支持用户查看局部细节数据
3. 浅色调专属体验优化
新增大屏自动轮播功能，支持设置轮播间隔，自动切换不同的分析页面，适配监控大屏场景
新增超标异常预警提醒：当污染物超标时，对应指标卡、图表出现轻量的边框高亮提醒，无过度花哨的动效，符合大厂专业产品的克制风格
优化加载状态：所有图表新增骨架屏加载效果，完全对齐阿里 / 字节的浅色调加载规范，避免页面空白
新增错误、空数据状态的友好提示，样式统一适配浅色调规范，提升用户体验
本部分验收标准
全局筛选功能正常，所有图表可联动刷新，无数据错误
图表的下钻、全屏、导出、缩放功能可正常使用，无兼容性问题
大屏轮播、预警提醒、加载状态等功能正常，交互体验完全对齐阿里 / 字节的商业化产品标准。
第五部分：工程化与性能保障优化（最后批次落地，为后续功能拓展铺垫）
本部分为前端代码层面的优化，完全对标大厂前端工程化标准，提升项目的可维护性、拓展性与加载性能，不影响任何业务功能的正常使用，可在所有业务优化完成后落地。
1. 前端工程化优化
样式管理：基于第一部分的设计规范，将通用样式、设计 token 统一封装为浅色调主题包，支持后续快速拓展主题，避免重复样式代码
组件封装：将通用的图表、卡片、按钮、表单控件封装为可复用的业务组件，统一全平台体验，提升后续功能开发效率
代码规范：拆分过大的 Vue 组件，按「页面 - 模块 - 组件」三级结构重构代码，统一代码规范，提升可维护性
接口封装：统一封装 API 请求，增加请求缓存、错误拦截能力，符合大厂前端工程规范
2. 大屏性能优化
图表渲染优化：实现图表懒加载，非首屏的图表在滚动到可视区域后再渲染，提升大屏首屏加载速度
数据缓存：在前端缓存已获取的接口数据，减少重复的 HTTP 请求，提升页面切换流畅度
渲染优化：针对大数据量的图表，开启 ECharts 的大数据渲染优化，避免页面卡顿，保证 60fps 流畅度
打包优化：基于 Vite 优化打包配置，开启代码分割、按需加载、压缩，减小构建产物体积
3. 兼容性与拓展性优化
多尺寸适配：优化响应式布局，完美适配 19201080、25601440、3840*2160 等主流大屏尺寸，保证所有场景下展示效果完整
路由结构优化：整理路由结构，预留后续新增功能的路由位置，支持快速新增页面
状态管理优化：引入 Pinia 统一管理前端状态，为后续新增的复杂功能做准备
本部分验收标准
通用组件已完成封装，可正常复用，代码结构清晰，符合大厂前端工程规范
大屏首屏加载速度提升 50% 以上，无卡顿、渲染延迟问题，大数据量图表渲染流畅
项目可正常构建打包，无代码报错，完美适配不同尺寸的大屏，为后续功能迭代预留了完整的拓展空间。